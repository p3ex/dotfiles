## Components:
  - tmux 
  - nvim
  - vscode
  - alacritty
  - zsh
  - hyprland (config only)

## OS packages:
    - cli utils (rg, fzf, nmap, pyenv, pipenv)
    - golang
    - rust, cargo
    -

## Features:
availability to install through wget / curl 
(wget setup.kube.gg )